name: Deploy frontend App

on:
  push:
    branches:
      - main
    tags:
      - 'v1.*'

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Log in to Docker Hub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Build Docker image
      run: docker build -t noscopev6/chatapp-b:$GITHUB_JOB .

    - name: Push Docker image
      run: docker push noscopev6/chatapp-b:$GITHUB_JOB
    
    # - name: Run Docker image 
      # run: docker run -d -p 5000:7000 ahmadrazalab/doc:$GITHUB_JOB